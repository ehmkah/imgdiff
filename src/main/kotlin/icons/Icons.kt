package icons

import com.intellij.openapi.util.IconLoader

import javax.swing.*

/**
 * @author Michael Krausse (ehmkah)
 */
interface Icons {
    companion object {

        val IMG_DIFF = IconLoader.getIcon("/icons/imgdiff.svg")
    }
}
